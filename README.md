# sbom-homework

## Requirements

- Python 3.10+
- Only standard library modules (no external dependencies)




## Ideas for Features
- Some repos put their `requirements.txt` files inside other folders, so it could be a good idea to do a recursive search.